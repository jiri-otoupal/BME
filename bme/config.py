from pathlib import Path

default_bookmarks_location: Path = (Path().home().resolve() / ".bme" / "bookmarks.json")

default_sequences_location: Path = (Path().home().resolve() / ".bme" / "sequences.json")

default_config_location: Path = (Path().home().resolve() / ".bme" / "config.json")
